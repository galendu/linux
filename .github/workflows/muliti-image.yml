name: 拉取固定cpu架构镜像到本地

on:
  workflow_dispatch:     
    inputs:
      oldimage_name:
        description: '镜像名称'
      imges_url:
        description: '镜像仓库'
        default: 'test'
      server_name:
        description: '服务名称'
      tag_name:
        description: '镜像tag'
      cpu_arch:
        description: '默认x86'
        default: ''
        
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - name: 拉取镜像并下载到本地
      run: |
        if [[ -z "${{ github.event.inputs.cpu_arch }}" ]]; then
               docker pull ${{ github.event.inputs.oldimage_name }}
          elif [[ -n "$string" ]]; then
              cpu_arch=${{ github.event.inputs.cpu_arch }}
        fi
        for i in ${{ github.event.inputs.oldimage_name }};
        do 
          docker pull $i --platform=${cpu_arch};
          name=`echo $i | awk -F ':' '{print $1}'|awk -F '/' '{print $NF}'`
          docker save $i -o $name.tar 
        done
        mkdir artifacts
        mv *.tar artifacts/
    - uses: actions/upload-artifact@v2
      with: 
        name: artifacts
        path:  artifacts/*
